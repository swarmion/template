{
  "root": "backend/users",
  "projectType": "application",
  "targets": {
    "deploy": {
      "executor": "@nrwl/workspace:run-commands",
      "outputs": [
        "backend/users/.serverless"
      ],
      "dependsOn": [
        {
          "target": "build",
          "projects": "dependencies"
        },
        {
          "target": "package",
          "projects": "dependencies"
        }
      ],
      "options": {
        "cwd": "backend/users",
        "commands": [
          {
            "command": "serverless deploy"
          }
        ]
      }
    }
  },
  "tags": [],
  "implicitDependencies": [
    "backend-core"
  ]
}
